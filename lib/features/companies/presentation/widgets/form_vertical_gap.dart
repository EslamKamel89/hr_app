import 'package:flutter/material.dart';

class FormVerticalGap extends StatelessWidget {
  const FormVerticalGap({super.key});

  @override
  Widget build(BuildContext context) {
    return SizedBox(height: 10);
  }
}
